import type {
  DomAdapter,
  MetricsOptions,
  MetricsSummary,
  NormalizedMetricsOptions,
  RenderMetric,
  TestingPluginOptions,
} from '@pokujs/dom';

export type VueDomAdapter = DomAdapter;
export type VueMetricsSummary = MetricsSummary;
export type VueMetricsOptions = MetricsOptions;
export type VueTestingPluginOptions = TestingPluginOptions;

export type {
  NormalizedMetricsOptions,
  RenderMetric,
};
